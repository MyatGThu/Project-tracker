{
  "name": "poker-tracker-api",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker API for Poker Tracker (D1-backed)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
